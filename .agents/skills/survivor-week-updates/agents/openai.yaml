interface:
  display_name: "Survivor Week Updates"
  short_description: "Update weekly data or initialize missing in-progress seasons"
  default_prompt: "Use $survivor-week-updates to add weekly eliminations, tribe changes, and advantages, or initialize a missing in-progress season file."

policy:
  allow_implicit_invocation: false
