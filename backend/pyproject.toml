[project]
name = "survivor-pool-api"
version = "0.1.0"
description = "API for the Survivor Pool App"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "fastapi>=0.116.1",
    "uvicorn>=0.35.0",
]
